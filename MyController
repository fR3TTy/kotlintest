import java.awt.event.ActionEvent
import java.awt.event.ActionListener

class MyController : ActionListener {
    override fun actionPerformed(e: ActionEvent) {
        val command = e.actionCommand
        if (command == "Send") {
            println("Send")
        } else if (command == "Close") {
            println("Close")
        }
    }
}
